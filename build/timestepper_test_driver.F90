program tests

  ! Driver program for Zofu unit tests in:
  ! /home/arthie/NaCL_EOS/waiwera/test/unit/src/timestepper_test.F90
  ! Generated by Zofu.

  use zofu_mpi
  use timestepper_test

  implicit none

  type(unit_test_mpi_type) :: test

  call setup()

  call test%init()

  call test%run(test_timestepper_read, 'Tests assigning timestepper parameters from JSON string.')
  call test%run(test_timestepper_linear, 'Linear function')
  call test%run(test_timestepper_exponential, 'Exponential function')
  call test%run(test_timestepper_logistic, 'Logistic equation')
  call test%run(test_timestepper_nontrivial_lhs, 'Nontrivial LHS')
  call test%run(test_timestepper_nonlinear_lhs, 'Nonlinear LHS')
  call test%run(test_timestepper_heat1d, '1-D heat equation PDE')
  call test%run(test_timestepper_heat1d_nonlinear, '1-D heat equation PDE (nonlinear form)')
  call test%run(test_timestepper_pre_eval, 'Pre-evaluation procedure problem')
  call test%run(test_timestepper_steady, 'Steady state solution of dy/dt = 1 - y2')
  call test%run(test_checkpoints, 'Timestepper checkpoints')

  call test%summary()
  call teardown()

  if (test%failed) stop 1

end program tests
